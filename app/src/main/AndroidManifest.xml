<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LITERISEMOBILE"
        tools:targetApi="31">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name="com.literise.ui.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.LITERISEMOBILE">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name="com.literise.ui.activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.LITERISEMOBILE" />

        <!-- Register Activity -->
        <activity
            android:name="com.literise.ui.activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.LITERISEMOBILE" />

        <!-- Original MainActivity (can be removed later) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>